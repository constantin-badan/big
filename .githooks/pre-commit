#!/bin/sh
set -e

echo "Running lint..."
npx nx affected -t lint

echo "Running typecheck..."
npx nx affected -t typecheck

echo "Running tests..."
npx nx affected -t test

echo "Running boundary checks..."
npx eslint 'packages/*/src/**/*.ts'

echo "All checks passed."
