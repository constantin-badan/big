{
  "name": "@trading-bot/risk-manager",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "test": "bun test",
    "typecheck": "tsgo --noEmit -p tsconfig.json",
    "lint": "oxlint ./src --config ../../.oxlintrc.json"
  },
  "dependencies": {
    "@trading-bot/types": "workspace:*",
    "@trading-bot/event-bus": "workspace:*"
  }
}
