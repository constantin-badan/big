export type {
  IStrategy,
  StrategyConfig,
  StrategyDeps,
  StrategyFactory,
  SignalMerge,
  SignalBuffer,
  SweepParamGrid,
} from '@trading-bot/types';
export { Strategy, passthroughMerge } from './strategy';
